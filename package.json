{
  "name": "survey-ai-root",
  "private": true,
  "scripts": {
    "build": "cd project && npm install && npm run build && cp -r dist ../Backend/dist",
    "start": "cd project && node server.cjs",
    "install:frontend": "cd project && npm install"
  }
}
