  const NAV_GROUPS = [
    { label: 'People',     items: [{ id: 'users',            icon: '👥', label: 'Users' }] },
    { label: 'Content',    items: [{ id: 'surveys',          icon: '📋', label: 'All Surveys' }, { id: 'survey-report', icon: '📊', label: 'Survey Report' }, { id: 'filters', icon: '🔍', label: 'Filters' }, { id: 'pass-fail', icon: '✅', label: 'Pass / Fail' }] },
    { label: 'Growth',     items: [{ id: 'referrals',        icon: '🎁', label: 'Referrals' }, { id: 'earnings-config', icon: '💰', label: 'Earnings' }] },
    { label: 'Engagement', items: [{ id: 'tracking',         icon: '📡', label: 'Tracking' }, { id: 'contacts', icon: '✉️', label: 'Contacts' }, { id: 'link-masking', icon: '🔗', label: 'Link Masking' }] },
    { label: 'System',     items: [{ id: 'location-control', icon: '📍', label: 'Location' }, { id: 'deletions', icon: '🗑️', label: 'Deletions' }, { id: 'survey-settings', icon: '⚙️', label: 'Survey Settings' }] },
  ] as const;

  const activeLabel = NAV_GROUPS.flatMap(g => g.items).find(i => i.id === activeTab)?.label || '';

  return (
    <ProtectedRoute requireAdmin>
      <div className="min-h-screen bg-[#f4f6fb] flex flex-col">
        <Header />

        {/* ── Top bar ── */}
        <div className="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between shrink-0">
          <div className="flex items-center gap-3">
            <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-indigo-600 to-purple-600 flex items-center justify-center text-white text-sm font-bold shadow">A</div>
            <div>
              <h1 className="text-base font-bold text-gray-900 leading-none">Admin Panel</h1>
              <p className="text-[11px] text-gray-400 mt-0.5">Pepperwahl Control Centre</p>
            </div>
          </div>
          <div className="flex items-center gap-3">
            <div className="hidden sm:flex items-center gap-4 text-xs text-gray-500">
              <span className="flex items-center gap-1.5"><span className="w-2 h-2 rounded-full bg-green-400 inline-block"></span>{users.length} users</span>
              <span className="flex items-center gap-1.5"><span className="w-2 h-2 rounded-full bg-blue-400 inline-block"></span>{surveys.length} surveys</span>
            </div>
            <button
              onClick={() => setShowNotifModal(true)}
              className="flex items-center gap-1.5 px-3 py-1.5 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg text-xs font-semibold transition-colors shadow-sm"
            >
              <Bell size={13} /> Send Notification
            </button>
          </div>
        </div>

        {/* ── Body: sidebar + content ── */}
        <div className="flex flex-1 overflow-hidden">

          {/* ── Sidebar ── */}
          <aside className="w-56 bg-white border-r border-gray-200 flex flex-col shrink-0 overflow-y-auto">
            <nav className="py-4 px-3 space-y-5 flex-1">
              {NAV_GROUPS.map(group => (
                <div key={group.label}>
                  <p className="text-[9px] font-bold uppercase tracking-widest text-gray-400 px-3 mb-1">{group.label}</p>
                  <div className="space-y-0.5">
                    {group.items.map(item => (
                      <button
                        key={item.id}
                        onClick={() => {
                          setActiveTab(item.id as typeof activeTab);
                          if (item.id === 'survey-settings') { loadPlatformConfig(); setSettingsSubTab('global'); }
                        }}
                        className={`w-full flex items-center gap-2.5 px-3 py-2 rounded-lg text-sm transition-all ${
                          activeTab === item.id
                            ? 'bg-indigo-50 text-indigo-700 font-semibold'
                            : 'text-gray-600 hover:bg-gray-50 hover:text-gray-900'
                        }`}
                      >
                        <span className="text-base leading-none">{item.icon}</span>
                        <span className="truncate">{item.label}</span>
                        {activeTab === item.id && <span className="ml-auto w-1.5 h-1.5 rounded-full bg-indigo-600 shrink-0" />}
                      </button>
                    ))}
                  </div>
                </div>
              ))}
            </nav>
          </aside>

          {/* ── Main content ── */}
          <main className="flex-1 overflow-y-auto p-6">

            {/* Page heading */}
            <div className="mb-5 flex items-center justify-between">
              <div>
                <h2 className="text-xl font-bold text-gray-900">{activeLabel}</h2>
                <p className="text-xs text-gray-400 mt-0.5">
                  {activeTab === 'users' && `${users.length} registered users`}
                  {activeTab === 'surveys' && `${surveys.length} surveys in system`}
                  {activeTab === 'filters' && `${filters.length} suggestion filters`}
                  {!['users','surveys','filters'].includes(activeTab) && 'Manage settings and configuration'}
                </p>
              </div>
            </div>

            {/* Content card wrapper */}
            <div className="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden">

              {loading ? (
                <div className="flex flex-col items-center justify-center py-20 gap-3">
                  <div className="animate-spin rounded-full h-10 w-10 border-[3px] border-indigo-200 border-t-indigo-600"></div>
                  <p className="text-sm text-gray-400">Loading…</p>
                </div>
              ) : (
                <>